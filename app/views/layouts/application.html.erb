<!DOCTYPE html>
<html>
  <head>
    <title>CourseRegistration</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark">
      <%= link_to "홈", home_path, class: "navbar-brand home" %>
      <div style="margin-right: 50px; margin-top:10px; margin-bottom: 10px">
        <% if current_user %>
          <%= link_to "로그아웃", destroy_user_session_path, method: :delete, class: "btn-outline-danger"%>
        <% else %>
          <%= link_to "로그인", new_user_session_path, class: "btn btn-outline-danger" %>
          <%= link_to "회원가입", new_user_registration_path, class: "btn btn-outline-danger"%>
        <% end %>
      </div>

    </nav>
    <div class="d-flex align-items-start ">
      <% if current_user %>
        <div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" >
          <br>
          <%= link_to "튜터 정보로 이동", tutors_path, class:"nav-link" %>
          <br>
          <%= link_to "수업 스케줄로 이동", tutor_schedules_path, class:"nav-link" %>
          <br>
          <%= link_to "수강신청 하로 가기", student_lessons_schedule_enter_path, class:"nav-link" %>
        </div>
      <% end %>
      <div class="ver-line"></div>
      <div class="content">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
